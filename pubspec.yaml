name: dino_evolution
description: "Dino Evolution - converted from Python/Pygame to Flutter/Flame"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.18.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
